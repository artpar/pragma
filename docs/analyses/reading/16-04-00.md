# Session 2026-09-24T16-04-00 — Worker on CMP-002 (per-model context window: 1_000_000 → 200_000 for glm53). 40 calls, COMPLETED; hit by mid-session AUTO-COMPACTION.

## Call-by-call narrative
- **C1–2**: agent.md; provider.go (ContextWindow=1_000_000); ContextWindow consumers.
- **C3–5**: Case record; hunts raw_isl_tokens 429 evidence: `~/.claude/skills` **missing** (payload location wrong); roadmap greps; `~/.pragma` scan.
- **C6**: **AUTO-COMPACTION fires mid-task**: input collapses 35758→2587, 78s call, out=11881 `<analysis>` conversation reconstruction — the very feature this loop builds. Re-orientation needed.
- **C7–8**: Re-orients ("I'm the WORKER for CMP-002"); re-greps what C2–4 had already found; 3-min wall gap after C7.
- **C8–12**: Evidence located: SKILL.md at `~/.pragma/skills/harness-self-evolution` (payload's `~/.claude` refuted); verbatim 429 line from 2026-09-11T18-50-33.jsonl (`current=291066, limit=200000`); peak input 206,838 in that log.
- **C13–19**: Wiring: BuildCompactionDeps ctxWindow default 200_000, provider override; llmconfig; postmortem 345k-context session; requestTokenCount prefers provider CountTokens.
- **C20–23**: Test conventions (TestMetadata asserts constant); queue diff study (orchestrator's uncommitted edits — must not commit them); 36s plan call.
- **C24–25**: Queue-state deep dive (input jumps ~57K→76K; 40s thinking) — establishes staging boundaries.
- **C26–27**: No other consumers of the constant; apply_patch RED gate on provider_test.go (per-model windows).
- **C28–33**: RED on unchanged tree (1000000 vs 200000); **the fix**: apply_patch provider.go per-model map; GREEN; full build; compact/query/cli suites; vet.
- **C34–37**: Case-record amendment: apply_patch **fails twice** (wrapped-line context mismatch; `cat -A` unsupported on macOS); re-reads tail; lands on third patch — 3 calls for one note.
- **C38–40**: Stages exactly 3 files (leaves queue untouched), commits 9066f39, final summary TEXT.

## BLINDSPOTS AND ISSUES
1. **Mid-task compaction + stall (C6–C8)**: its own harness compacted the context (78s) and there's an unexplained 3-minute gap after C7; the worker re-derived basics (C7 re-greps) — context loss cost ~2–3 calls and risk.
2. **Evidence-location detour (C3–5 → C15–17)**: payload pointed at `~/.claude/skills`; actual location `~/.pragma/skills` found only at C15 — ~6 calls of hunting for one string.
3. **Patch retries (C34–37)**: 2 failed hunks + unsupported `cat -A` — 3 calls to append one note.
4. Finished correctly: RED→GREEN, precise staging, queue left for orchestrator.

## Efficiency verdict
Correct, well-gated fix despite losing context to its own compaction feature and a 3-min stall. Waste: ~6 calls evidence archaeology + 3 patch-retry calls.

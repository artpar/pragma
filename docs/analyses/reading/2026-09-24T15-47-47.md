# Session 2026-09-24T15-47-47 — Worker, queue item CMP-001.4c (BuildCompactionDeps calibration, zero-window guard, reserve estimation)

72 API calls, 15:47:51 → 15:57:21 (~9.5 min), context 1,508 → 110,323 in-tokens (no compaction). Commit: 9cc18ae (3 production files + tests + case record + queue).

## Call-by-call narrative

- 1–2: agent.md; payload CMP-001.4c (F8: SystemPromptEst uses PragmaLoopSystemPrompt for both loop modes; zero WindowConfig → threshold 0).
- 3–8: Verifies BuildCompactionDeps (run.go:2029) computes sysTokEst from `PragmaLoopSystemPrompt()` only; SetCompaction sites; window.go; auto.go; provider-tools system = `snap.Conversation.System`.
- 9–11: Finds dead parent's untracked RED gates (compaction_window_calibration_test.go, auto_zero_window_test.go); notes prior F6 requestTokenCount already fixed.
- 12–16: System-prompt composition in both loops (custom + pragmaLoopSystemPrompt in pragma mode; custom via WithCustomSystemPrompt in provider-tools); engine request view.
- 17–19: Zero-window provenance: no provider returns (0, true) for ContextWindow, but `--max-tokens` defaults 0 → MaxOutput=0 possible.
- 20–25: Tool schemas not counted in estimate (providerToolDefs = Bash+apply_patch, plus websearch/agent tools); ApplyResult preserves Conversation.System; Deps.EngineCfg carries LoopMode; RegisterTools sets d.Engine before BuildCompactionDeps (all 3 sites).
- 26–33: Existing-test inventory (no zero-window users; property test uses configured window); imports available in run.go.
- 28–29: **RED confirmed both pre-drafted gates** (zero-window; calibration) with exact failure output (call 29: 5,680-token thinking).
- 34–38: Fix: zero-window guard in ShouldAutoCompact; `EstimateCompactionReserve` on Engine; calibration to active loop mode in run.go BuildCompactionDeps.
- 39–41: Comment-indentation self-caught and fixed; build + calibration gate GREEN.
- 42–48: New reserve gate test file written via `cat > file <<EOF` then patched for imports; unused websearch import removed; vet/gofmt iterated.
- 49–56: **RED-sensitivity demonstration via stash + python-neutering of engine.go**: gate is structurally tied to new method, so it stashes the fix and python-neuters engine.go to simulate old estimator; **heredoc swallowed the `cp` restore — tree stayed neutered** while tests puzzlingly still returned 270 (calls 51–55 confusion: "restore didn't work?" → "git diff truncated by head" → "I see! the NEUTERED version"); restore via cp; GREEN.
- 57–58: `-race`; full suite (only documented pre-existing TestProviderToolsCLIContract failure).
- 59–62: **Second stash cycle**: push to check pre-existing gofmt state; **stash pop FAILS silently once** — agent: "the earlier failed pop was probably due to... whatever, maybe the /tmp engine restore raced" — **unexplained error explicitly dismissed**; second pop succeeds; gates re-verified green.
- 63–65: gofmt -l lists files; decides most are pre-existing unformatted; **third stash + python-neuter to fix gofmt on its own engine.go** (its earlier patch left misformat); restores.
- 66: Build + full gates green from fixed tree.
- 67–69: Case-record revision appended; queue status updated via python heredoc.
- 70–72: Commit 9cc18ae; final sanity from committed state; report.

## Blindspots and issues

1. **RED-after-fix forced a ~15-call stash/neuter detour (calls 49–65):** because the reserve gate was written *after* the fix and referenced the new method, RED had to be simulated by stashing production code and python-neutering engine.go. Two restore failures followed (heredoc swallowing `cp`; a failed `git stash pop`), each triggering multi-call confusion loops — real risk of committing a neutered engine.go. Root cause is a methodology violation: write the gate first, run RED, then fix (as this same agent did for mechanism B in 15-30-33).
2. **Errors absorbed without diagnosis:** the failed stash pop at 61 was waved off as "whatever"; the truncated git diff at 53–54 was guessed at ("truncated by head") before the real cause (file still neutered) surfaced at 55. Pattern: when a shell result contradicts expectations, the agent hypothesizes truncation first, state second.
3. **Source mutated via python heredocs** (neutering at 50, 65; gofmt fix at 65) instead of apply_patch — outside the mandated editing method and a direct contributor to the restore failures.
4. **Repeated self-formatting errors:** comment misindent caught at 39; gofmt drift found at 63–65 — running `gofmt -l` immediately after each patch (as 21-17-12 did at 47) would have avoided both.
5. **Some double-derivation in verification:** calls 12–16 partially re-derive system-prompt composition already read at 3–9.

## Efficiency verdict

Final state is correct and the gate set is strong, but this is the cohort's riskiest session: ~20% of calls burned on stash/neuter gymnastics caused by writing RED gates after the fix, with two near-miss work losses and one error explicitly dismissed as "whatever." Write gates before fixes and keep all source edits in apply_patch.

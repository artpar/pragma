# Session 2026-09-24T23-08-34 — Critic, audit of commit c9984e1 (CMP-001.4a.F1, fork engines no longer inherit root SessionCheckpoint)

57 API calls, 23:08:36 → 23:16:34 (~8 min — the shortest active session), context 1,520 → 66,274 in-tokens (lowest total input of the 7: 2.46M vs cohort 3.4-5.4M). No commit; /tmp audit worktrees removed at end.

## Call-by-call narrative

- 1–5: git show c9984e1 (engine.go diff, test file 2 chunks, record diff).
- 6–9: EngineConfig value-copy semantics verified (subCfg.SessionCheckpoint = nil affects only the fork); makeSessionSaveClose walked.
- 10: 39s/5.3k think: pre-fix fork-append write path synthesis.
- 11–22: all fork sites route through ForkFreshConversation (orchestration ×2, subagent ×1 — one-mechanism claim holds); rewriteSession unreachable on forks (compactor nil); SessionsDir/store/writer resolution.
- 13–19: **dead-config discovery**: RecordContentReplacements + ApplyToolResultBudget wired but never invoked in production (git -S archaeology) — related-but-out-of-scope, correctly quarantined rather than expanded into.
- 23–25: WriteMessage dedups by ID → test 2's "double-wrote" failure text can never fire via double-write (subtle assertion-honesty check).
- 26–30: creates /tmp/audit-green (c9984e1) and /tmp/audit-red (parent) worktrees; TestFork green at commit; RED at parent with the exact recorded failure text.
- 31–34: **compound mutation test**: python-heredoc reintroduces fork checkpoint + removes F5 lock in audit-red → -race gate fails with genuine DATA RACE and the functional 36≠22 assertion fires — proves test 2 has real regression power despite passing pre-fix.
- 35–39: close-time save (run.go:1140) covers artifact legs; upsertOrchestrationArtifact is in-memory; Rewrite writes metadata → metadata==0 assertion is live.
- 39–41: CLIContract fails at commit AND pristine parent — pre-existing, honestly disclosed.
- 42–45: EventBus delivers asynchronously via dispatch goroutine → `recorder.count()` immediately after Emit can race → flake risk in the committed test identified from code.
- 46–51: **empirical flake hunt**: -count=30 -race pass; **GOMAXPROCS=1 ×40 → test 1 line 271 fails ~97%** ("SessionSaved events = 0, want 1"); isolated test 2 ×60 passes; test 1 at default GOMAXPROCS flakes ~4/60 (≈7%) — a real, reproducible flake shipped in the commit.
- 52–54: record heading style cosmetic; vet/gofmt/query green; severity pinned.
- 55–57: worktree cleanup; notices main repo HEAD advanced to 3d2a6ba (other commits landed mid-audit) — verifies c9984e1 ancestry unaffected; end-turn report.

## Blindspots and issues

1. **Mutation hygiene risk (32–33):** compound mutation applied via python heredoc directly into audit-red's run.go with no backup copy first (unlike 21-59-22 which at least kept .bak files but still leaked) — a crash mid-mutation would have left the audit tree corrupted; restore path existed but only via worktree re-add.
2. **/tmp/audit-red provenance (29):** uses /tmp/audit-red without a visible creation call (created inside 27's truncated compound command) — fine in practice, but the render shows the same &&-chain opacity that bit two sibling sessions.
3. Positives: the tightest token budget of the cohort with the highest finding density (7% flake at line 271, compound-race proof, dead-config quarantine); worktrees removed; repo left clean; no shell-rule workarounds in the REAL repo.

## Efficiency verdict

Best efficiency of the 7: 8 minutes, 2.46M input tokens, one real committed-test flake proven empirically plus a compound mutation proving the race gate's power.
Only nits are mutation-backup discipline and inherited compound-command opacity — nothing wasted beyond ~2 calls.

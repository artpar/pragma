# Session 2026-09-24T16-30-26 — MCP-injection e2e PROBE (2 calls; inside unattended2 c1 worker)

Role: end-to-end harness probe launched during unattended2 cycle-1 CLK-002 worker session 16-22-55 (which subsequently cap-died at exactly 80 calls, exit=1, no commit). "PROBE_COMPLETE" marker captured in .self-improve/20260924-162255-c1-worker.log.

## Per-call narrative
- **1** (16:30:41, 15.0s — stub-provider latency): `Bash: echo MCP_INJECTION_PROBE_BASE_PATH_OK` + `mcp__past-conversations__list_projects {}` (verifies MCP servers block and tool dispatch on the candidate build).
- **2** (16:30:41, 10ms): TEXT "PROBE_COMPLETE", `end_turn`. Usage reported in=1/out=1 (e2e stub provider's canned accounting).

## BLINDSPOTS AND ISSUES
1. In-session: none — textbook probe; wire capture feeds e2e/mcp-injection assert_boot.py.
2. Systemic: the probe succeeded while its parent worker cycle failed — the CLK-002 worker passed its e2e probe mid-flight yet still consumed all 80 turns and died without commit (attempt 2 requeued). The probe validates a code path, not the worker's budget discipline.

## Efficiency verdict
2 calls / instant / trivial cost — perfect.
Verdict: flawless; the failure belonged to the parent worker, not the probe.

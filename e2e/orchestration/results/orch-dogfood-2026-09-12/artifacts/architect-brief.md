Goal:
Adversarially review the TUI-001 mechanism (range 413122a^..948431c) and verify the four concrete behavioral claims, then record findings. This is review-only: no repository file may be edited, created, or deleted; all deliverables go under /tmp/pragma/.

Relevant files:
- internal/tui/model.go — segment struct with forceShow, promoteTrailingThinking (lines ~514-539), isThinkingOnlyAssistant (~541-560), viewportContent rendering at ~432 which ORs m.verbose || seg.forceShow into render.RenderThinking.
- internal/tui/handlers.go — the three production mutation sites: TextEvent sets finalResponseHadText (~253-256), ToolResultEvent resets it to false (~383-388), TurnCompleteEvent promotes only when e.StopReason == model.StopEndTurn && !m.finalResponseHadText (~440-443); submitPrompt resets it (~768-771); reloadConversationFromStore (~117-129) reloads segments and promotes when the last conversation message is a thinking-only assistant message.
- internal/tui/thinking_turn_test.go — 165 lines, four gates: TestThinkingOnlyEndTurnShowsInstruction, TestThinkingThenTextStaysCollapsed, TestTextlessFinalResponseAfterToolWorkIsPromoted, TestReloadShowsTrailingThinkingOnlyMessage. Uses newTestModel from e2e_test.go and ansiCodes regexp from queued_prompt_test.go.
- internal/cli/run.go:240-277 — the queued-prompt producer: RunInput appends the user message to the conversation and emits QueuedPromptEvent when a turn is already running.
- internal/tui/handlers.go:180-189 — the QueuedPromptEvent consumer: renderAcceptedPrompt appends a rendered user-message segText (via render.RenderMessage, ends with "\n\n"), then appendText adds "· queued behind the running turn..." + "\n\n".
- internal/query/provider_tools_loop.go:104-123, 435-448 — emits ThinkingEvent per ThinkingPart (skipping empty text), then TurnCompleteEvent{StopReason: response.StopReason}.
- internal/tui/render/content.go:176-189 — RenderThinking collapsed form "∴ Thinking (ctrl+o to expand)"; the exact assertion strings.
- internal/model/stop.go — stop reason constants, including StopToolUse, StopPauseTurn, StopMalformedToolCall, StopContentFiltered.
- internal/tui/model.go:493-504 — appendText merges consecutive segText into one segment; important for understanding the segment-scan stopping behavior.
- internal/tui/messages.go-equivalent loadMessageSegments in model.go:832-920 — how reload builds segments: thinking parts become segThinking, tool calls become segTool/segGroup, other parts become segText; appends trailing "\n" text segment at end.

Required changes:
- Verify claim 1 — text-bearing final responses keep thinking collapsed.
  from contract: task check (1); commit 413122a mechanism "text-bearing responses keep thinking collapsed".
  approach: run go test ./internal/tui -run 'TestThinkingThenTextStaysCollapsed|TestReloadShowsTrailingThinkingOnlyMessage' -count=1 -v. Confirm the finalResponseHadText flag is only set by TextEvent with non-whitespace content (handlers.go:253-256) and never cleared mid-response. Confirm nothing else sets forceShow (grep forceShow in internal/tui/*.go, non-test, yields only model.go:149 declaration, model.go:432 render read, model.go:536 assignment inside promoteTrailingThinking). Also check the edge where TextEvent delivers whitespace-only text — flag stays false and promotion would fire; decide whether that matches the documented intent (doc says "whitespace alone does not" count as operator-readable — intentional).
- Verify claim 2 — reload promotion fires only when the last conversation message is an assistant message whose content parts are all thinking blocks.
  from contract: task check (2); isThinkingOnlyAssistant definition and reloadConversationFromStore.
  approach: run TestReloadShowsTrailingThinkingOnlyMessage -v; also test the boundary inputs: last message with empty content slice (len 0 -> false, no promotion), user-role message with all-thinking content (false), assistant message whose content is thinking+text (false — covered by the reload gate's textBearing case). A quick throwaway Go test under /tmp (not in repo) exercising isThinkingOnlyAssistant directly via a package-external copy is not possible (unexported); instead rely on the reload gate plus reasoning over handlers.go:124-127 and model.go:541-560. Additionally note a subtlety: the promotion in reloadConversationFromStore runs promoteTrailingThinking over the ENTIRE reloaded outputSegs, not just the last message's segments — so if the trailing message is thinking-only but an earlier assistant message in the conversation ends with thinking and an empty text tail, earlier thinking could also be promoted if contiguous; in practice loadMessageSegments appends "\n" (a whitespace text segment) after each message, which stops the backward scan — verify with a /tmp scratch test or by reasoning over appendText/loadMessageSegments. This scan-wide behavior is an acceptable finding to record, not a blocker if gated tests pass.
- Verify claim 3 — a mid-turn queued user message between thinking segments of the final response.
  from contract: task check (3); INT-001 interaction.
  approach: this is the adversarial gap: no existing gate covers QueuedPromptEvent interleaved with ThinkingEvents of the final response. The mechanism: ThinkingEvent(A) -> QueuedPromptEvent -> ThinkingEvent(B) -> TurnComplete(end_turn, no text). Because the queued prompt appends non-whitespace segText ("❯ ...", "· queued behind the running turn..."), promoteTrailingThinking's backward scan stops at that text segment — only ThinkingEvent(B) gets forceShow, ThinkingEvent(A) stays collapsed. That is "lost promotion" for the earlier thinking segment of the SAME final response (segment A is part of the same response). Record: promotion partially fires; earlier same-response thinking stays collapsed. Is that over-promotion? No. Is it lost promotion? Yes — partially. Whether it is a BLOCK depends on whether the response's full thinking was meant to be shown; at minimum it is a real finding. Verify by writing a throwaway test outside the repo (e.g., /tmp/pragma/queued_mid_thinking_test.go cannot import unexported internals; instead replicate the model flow via the existing test harness in a copied test file run with `go test` using a temporary overlay? — that requires repo modification). The safe verification: run a scratch program is not feasible for unexported Model. Therefore verify via reasoning plus running the existing suite (go test ./internal/tui -count=1) and record the uncovered adversarial case as a test-coverage gap with a suggested test body the implementer can include in /tmp/pragma/implementer-report.md (not in the repo). Optionally use `go test -run TestQueuedPromptEventRenders -v` to confirm the queued segment text is non-whitespace segText, which is the fact that stops the backward scan.
- Verify claim 4 — only end_turn promotes; other stops keep notice behavior.
  from contract: task check (4).
  approach: run focused tests and inspect handlers.go:429-443: promotion is guarded by e.StopReason == model.StopEndTurn; max_tokens, content_filter, and error each append their own bracketed notice and are not promoted (the promotion line requires StopEndTurn, so no double behavior). Also check the untested stop reasons: StopToolUse, StopPauseTurn, StopMalformedToolCall cannot reach the TurnCompleteEvent branch as final stop reasons in the provider tools loop (provider_tools_loop.go:127-129: if StopReason != StopToolUse && len(toolCalls)==0 -> TurnComplete; otherwise emits tool-call events or errors) — so only end_turn, max_tokens, content_filter, error can appear at TurnCompleteEvent. Verify with grep that TurnCompleteEvent{ is emitted with StopEndTurn in orchestration/runner.go (229-233, 256-259) and miniswe_loop.go (333, 397, 434) — orchestration emissions with StopEndTurn and no prior TextEvent would trigger promotion of trailing thinking from an orchestration run's outputs; check whether orchestration emits thinking segments. If orchestration emitted only text notices/lines (segText non-whitespace), promotion is a no-op there; verify by grepping OrchestrationEvent handlers in handlers.go for segThinking appends (the ThinkingEvent case is the only appendThinking caller; also loadMessageSegments). Record if orchestration could leave thinking behind.
- Run the required validation and record outputs.
  from contract: task instruction "Run go test ./internal/tui -count=1".
  approach: run `go test ./internal/tui -count=1 -v > /tmp/pragma/tui-test-output.txt 2>&1` (or -count=1 without -v; keep full output; -v output is long — redirect to a file under /tmp/pragma and print only a tail/summary). Also run `go build ./...` for sanity? Not required; the suite passing implies compile. Optionally `go vet ./internal/tui`. Do not run the full 29-package suite unless cheap; the task says internal/tui tests. Do not start servers or watchers.
- Write the brief for the implementer: the implementer's job is the review report.
  from contract: task instruction "the implementer writes findings to /tmp/pragma/implementer-report.md".
  approach: the implementer must write /tmp/pragma/implementer-report.md containing: verdict per check (1)-(4) with PASS/FAIL and evidence (test names, line numbers, commands and exit codes), the uncovered adversarial case (mid-turn queued prompt between thinking segments), any test-coverage gaps, and the orchestration-path scan. No repository edits allowed.

Deferred or out of scope:
- Changing the mechanism, adding tests to the repo, editing any repository file, or "fixing" the queued-prompt interleaving gap. Review-only task.
- Investigating the ORCH-001/ORCH-002 orchestration gates (f4242a0, 948431c) beyond the specific question of whether orchestration emissions could leave trailing thinking that gets promoted or block promotion — name it, do not investigate deeply.
- The MCPINJ e2e probe (probe_provider.py) and e2e/orchestration artifacts in the same range — adjacent commits, not part of the TUI-001 mechanism.
- Full-suite run (29 packages) and -race runs; the TUI-001 commit message mentions them but the review's contract is internal/tui.
- The runtime trace-instrumentation changes in 948431c (observe.GlobalTrace lines) — they are instrumentation only; the diff from 413122a to 948431c for the three in-scope files is purely added observe.GlobalTrace lines plus removed TUI-001 comments, no behavior change. Confirm with `git diff 413122a 948431c -- internal/tui/` that no logic lines changed; if confirmed, review the mechanism at 948431c state (identical behavior to 413122a for these files).

Validation:
- go test ./internal/tui -count=1 (already run once by architect: exit 0, "ok github.com/artpar/pragma/internal/tui 3.380s"; rerun after any steps)
- go test ./internal/tui -count=1 -run 'TestThinkingOnly|TestThinkingThenText|TestTextlessFinal|TestReloadShowsTrailing' -v (record pass/fail per test)
- go test ./internal/tui -count=1 -run 'TestQueuedPromptEventRenders' -v (confirms queued prompt text is rendered as operator-visible text, i.e., a non-whitespace segText segment that halts the backward thinking scan)
- go diff review: git diff 413122a 948431c -- internal/tui/model.go internal/tui/handlers.go internal/tui/thinking_turn_test.go | grep -v observe.GlobalTrace to confirm no logic change between the two commits.

Implementation cautions:
- Review-only: do not modify internal/tui/*.go or any repo file, do not add tests to the repo, do not create /tmp scratch Go files that import unexported symbols via repo-internal test packages (that requires a repo file).
- The observe.GlobalTrace lines added in 948431c are build-time trace injection; they are inert for behavior; do not mistake their presence for a changed mechanism.
- ansiCodes regexp strips SGR escapes; assertion strings like "ctrl+o to expand" contain styled spans — the collapsed hint assertion strips codes and works, but any new assertion must strip too.
- promoteTrailingThinking is in-place on a shared slice: it mutates segs[i].forceShow directly; safe because segments are values in a slice owned by outputSegs.
- The test harness newTestModel (internal/tui/e2e_test.go:23-50) has no runInput; LoopEventMsg events are injected directly through Update, which is the correct production path.
- If a run fails, capture full output to /tmp/pragma/ and print a tail; do not truncate validation output as proof.

Risks:
- The queued-mid-thinking case (check 3) is genuinely uncovered: the first thinking segment of the final response stays collapsed while the later one is promoted — a partial promotion. Decide BLOCK vs APPROVE: the task says "causes neither over-promotion nor lost promotion"; if the review finds partial loss of promotion for the earlier segment of the same final response, that is a deviation to weigh. Note mitigating fact: the queued prompt's own text ("· queued behind the running turn...") is rendered and tells the operator something was appended, and the collapsed hint still shows (ctrl+o). Whether the earlier same-response thinking is "lost" depends on the intent that the whole response's thinking should be visible; the mechanism documents "trailing run" only, so the doc-consistent reading is that only the trailing run is promoted by design. Weigh both readings in the report.
- Orchestration-runner emissions of TurnCompleteEvent{StopEndTurn} with no TextEvent in between (runner.go:233, 258) will trigger promoteTrailingThinking over whatever segments the orchestration handlers left; if an orchestration run appended only non-whitespace text segments, promotion no-ops (scan stops at text). No thinking is appended by orchestration handlers (only ThinkingEvent calls appendThinking); so no false promotion. Low risk.
- miniswe_loop.go also emits TurnCompleteEvent{StopEndTurn} — same no-op reasoning if no ThinkingEvents preceded.
- Reload path promotes over all reloaded segments, not just the last message's; if the last message is thinking-only but an earlier assistant message ended with thinking, the trailing "\n" text segment after each message in loadMessageSegments stops the scan, so no cross-message promotion. Verify with the reload gate which includes a preceding user message.
- StopToolUse and StopPauseTurn cannot reach the promotion line via the provider tools loop as a final stop reason; only end_turn/max_tokens/content_filter/error can (provider_tools loops errors out on malformed tool calls). This supports claim 4 holding in practice.
